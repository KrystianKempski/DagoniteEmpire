@page "/barony/budget"
@layout BaronyLayout
@attribute [Authorize(Roles = SD.Role_Admin + "," + SD.Role_GameMaster + "," + SD.Role_DukePlayer)]
@inject NavigationManager Nav

@code {
    protected override void OnInitialized() =>
        Nav.NavigateTo("/barony/resources#budget", replace: true);
}
